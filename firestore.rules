rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // UI tests run against emulator only and need unrestricted test writes.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
